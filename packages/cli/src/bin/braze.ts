#!/usr/bin/env node
import { errorCodes } from "brazecli-core"

// Placeholder entry point. Command registration lands in Phase 1 — docs/plans/.
process.stdout.write(`brazecli (scaffold) · ${errorCodes.length} error codes defined\n`)
