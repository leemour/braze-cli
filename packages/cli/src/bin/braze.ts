#!/usr/bin/env node
import { errorCodes } from "@braze-cli/core"

// Placeholder entry point. Command registration lands in Phase 1 — docs/plans/.
process.stdout.write(`braze-cli (scaffold) · ${errorCodes.length} error codes defined\n`)
